#!/bin/bash
######### SCript downloads samples directory from judge1 to /tmp in control
if [ -d /tmp/samples ]
then
	rm -Rf /tmp/samples
fi
scp -r root@judge1:/home/judge1/Desktop/samples /tmp
exit 0
